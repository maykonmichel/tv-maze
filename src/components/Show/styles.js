import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1 / 2,
    alignItems: 'center',
    marginVertical: 10,
  },
  image: {
    width: 180,
    height: 253,
  },
});
