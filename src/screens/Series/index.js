import React, {memo} from 'react';
import {Text, View} from 'react-native';

const Series = () => {
  return (
    <View>
      <Text>Series</Text>
    </View>
  );
};

Series.propTypes = {};

Series.defaultProps = {};

export default memo(Series);
